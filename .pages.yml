collections:
  - name: series
    label: المسلسلات والأنمي
    # هنا، type: collection يتم تعريفه في سياق أعلى (تحت collections)
    path: src/content/series
    format: json
    
    # 🌟 هنا يجب أن تبدأ بتعريف حقول البيانات الفعلية 🌟
    schema: # 💡 قد يكون هذا هو المكان الصحيح لتعريف الحقول
      fields:
        - name: title
          label: عنوان السلسلة
          type: string # ✅ الآن يمكنك استخدام الأنواع الأساسية
        - name: slug
          label: الرابط (Slug)
          type: string

        # ----------------------------------------------------
        # 1. قائمة المواسم: نوع الحقل الآن هو 'array' (أو 'list') 
        - name: seasons
          label: المواسم
          type: array # 💡 تجربة array مجددًا في هذا المستوى
          items: # 💡 تجربة items مجددًا في هذا المستوى
            type: object # كل عنصر في القائمة هو كائن (موسم)
            fields:
              - name: season_number
                label: رقم الموسم
                type: number
              
              # 2. قائمة الحلقات المتداخلة
              - name: episodes
                label: الحلقات
                type: array
                items:
                  type: object
                  fields:
                    - name: episode_number
                      label: رقم الحلقة
                      type: number
                    - name: title
                      label: عنوان الحلقة
                      type: string