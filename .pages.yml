collect:
  - name: series
    label: المسلسلات والأنمي
    path: src/content/series
    format: json
    fields:
      - name: title
        label: عنوان السلسلة
        type: string
      - name: slug
        label: الرابط (Slug)
        type: string

      # ----------------------------------------------------
      # 1. قائمة المواسم (المجموعة الخارجية)
      - name: seasons
        label: المواسم
        type: collection # 💡 محاولة استخدام 'collection' بدلاً من 'array' أو 'list'
        fields: # 💡 محاولة استخدام 'fields' بدلاً من 'items'
          - name: season_number
            label: رقم الموسم
            type: number
          - name: title
            label: عنوان الموسم
            type: string

          # ------------------------------------------------
          # 2. قائمة الحلقات (المجموعة الداخلية)
          - name: episodes
            label: الحلقات
            type: collection # 💡 استخدام 'collection' لتمثيل قائمة الحلقات
            fields:
              - name: episode_number
                label: رقم الحلقة
                type: number
              - name: title
                label: عنوان الحلقة
                type: string
              - name: video_url
                label: رابط الفيديو
                type: string